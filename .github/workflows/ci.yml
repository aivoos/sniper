# Polisi / jaksa / hakim: merge hanya jika hijau
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Verify modules
        run: go mod verify

      - name: Format (no diff)
        run: |
          test -z "$(gofmt -l . | tee /dev/stderr)"

      - name: Vet
        run: go vet ./...

      - name: Test + coverage (race: ubuntu has gcc)
        run: |
          CGO_ENABLED=1 go test ./... -count=1 -race -coverprofile=coverage.out -covermode=atomic

      - name: Coverage floor (adjust COVERAGE_MIN in repo if needed)
        env:
          COVERAGE_MIN: "55"
        run: |
          pct=$(go tool cover -func=coverage.out | grep '^total:' | awk '{gsub(/%/,"",$3); print $3}')
          echo "total coverage: ${pct}% (min ${COVERAGE_MIN}%)"
          awk -v p="$pct" -v m="$COVERAGE_MIN" 'BEGIN { exit !(p+0 >= m+0) }' || { echo "::error::coverage below ${COVERAGE_MIN}%"; exit 1; }

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage.out
          path: coverage.out
